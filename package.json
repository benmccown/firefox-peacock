{
  "name": "firefox-peacock",
  "version": "1.0.0",
  "description": "Change the color of your Firefox frame.",
  "repository": "https://github.com/areknawo/ProductivityBooster",
  "author": "Benjamin McCown <moosebeandev@gmail.com> (https://github.com/benmccown)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf ./node_modules ./dist",
    "build": "rm -rf ./dist && tsc && cp ./manifest.json ./dist/manifest.json && cp -Rf ./assets ./dist/assets",
    "release": "yarn clean && yarn install && yarn build"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.12.0",
    "typescript": "^4.6.3",
    "ts-loader": "^9.2.8",
    "web-ext-types": "^3.2.1",
    "yarn": "*"
  }
}
